{
    "resource": [
        {
            "null_resource": [
                {
                    "app1": [
                        {
                            "provisioner": [
                                {
                                    "local-exec": {
                                        "command": "echo 'Arrancando servidor ${var.name} en red ${var.network} en ${var.port} con ${var.api_key}'"
                                    }
                                }
                            ],
                            "triggers": {
                                "name": "${var.name}",
                                "network": "${var.network}",
                                "port":"${var.port}",
                                "api_key":"${var.api_key}"
                            }
                        }
                    ]
                }
            ]
        }
    ]
}